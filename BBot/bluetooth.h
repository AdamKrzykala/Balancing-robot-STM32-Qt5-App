#ifndef BLUETOOTH_H
#define BLUETOOTH_H

#include <QThread>
#include <QDebug>

// -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

struct Data_from_Robot
{

};

// -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

struct Data_to_Robot
{

};

// -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

class Bluetooth : public QThread
{
    Q_OBJECT

private:

    Data_from_Robot DF_Robot;
    Data_to_Robot   DT_Robot;

    void (Bluetooth::*f)();

    void Parse_frame();

public:

    Bluetooth();

    void run() {

        (this->*f)();
    }

    void Parse_frame_thread();

private slots:

public slots:

signals:

    void Parsed_Frame_OK();
    void Parsed_Frame_FAIL();
};

// -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

#endif // BLUETOOTH_H
